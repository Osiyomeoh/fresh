import { GetRoleRestrictedDataArguments, RoleRestrictedData, UpdateRoleRestrictedDataArguments, App } from '@slashauth/types';
import { WrappedClient, SlashauthResponse } from '../client';
import { Controller } from './controller';
export declare class AppController extends Controller {
    constructor(client_id: string, client_secret: string, apiClient: WrappedClient);
    getInfo(): Promise<SlashauthResponse<App>>;
    getRoleRestrictedData({ role, }: GetRoleRestrictedDataArguments): Promise<SlashauthResponse<RoleRestrictedData>>;
    updateRoleRestrictedData({ role, metadata, }: UpdateRoleRestrictedDataArguments): Promise<SlashauthResponse<RoleRestrictedData>>;
}
