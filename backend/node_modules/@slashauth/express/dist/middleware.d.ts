import { SlashauthClient } from '@slashauth/node-client';
import * as express from 'express';
declare global {
    namespace Express {
        interface Request {
            slashauth: {
                wallet?: string;
                userID?: string;
                isAuthed?: boolean;
                clientID?: string;
                getWalletAddress(): Promise<string | null>;
            };
        }
    }
}
export declare class SlashauthMiddlewareExpress {
    client: SlashauthClient;
    constructor(client: SlashauthClient);
    parseAuthToken: () => (req: express.Request, res: express.Response, next: express.NextFunction) => Promise<void>;
    hasRole: (role: string) => (req: express.Request, res: express.Response, next: express.NextFunction) => Promise<express.Response<any, Record<string, any>> | undefined>;
}
