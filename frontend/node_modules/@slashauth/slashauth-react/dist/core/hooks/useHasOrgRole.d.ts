type HasOrgRoleArgs = {
    organizationID: string;
    role: string;
};
export declare function useHasOrgRole({ organizationID, role }: HasOrgRoleArgs): {
    refetch: () => Promise<void>;
    organizationID: string;
    role: string;
    loading: boolean;
    hasOrgRole: boolean;
    error: Error;
};
export {};
