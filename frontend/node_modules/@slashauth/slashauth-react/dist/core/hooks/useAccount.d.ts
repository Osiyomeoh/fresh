import { Provider } from '@wagmi/core';
import { Account } from '../../shared/global';
import { SlashAuthWeb3ListenerPayload } from '../../shared/types';
export type AccountStatus = {
    wallet?: Omit<Omit<SlashAuthWeb3ListenerPayload, 'signer'>, 'provider'>;
    account?: Account;
    provider?: Provider;
};
export type UseAccountConfig = {
    onConnect?({ wallet, provider }: AccountStatus): void;
    onDisconnect?(): void;
};
export declare function useAccount({ onConnect, onDisconnect, }?: UseAccountConfig): AccountStatus;
