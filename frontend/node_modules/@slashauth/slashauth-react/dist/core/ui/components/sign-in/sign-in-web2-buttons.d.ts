import { LoginMethod } from '../../context/login-methods';
declare type Props = {
    onClick: (loginMethod: LoginMethod) => void;
};
export declare const SignInWeb2Buttons: ({ onClick }: Props) => JSX.Element;
export {};
