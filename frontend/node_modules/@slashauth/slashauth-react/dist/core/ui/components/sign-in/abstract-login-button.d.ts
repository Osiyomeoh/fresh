/// <reference types="react" />
declare type Props = {
    disabled?: boolean;
    additionalStyles?: React.CSSProperties;
    children: React.ReactNode;
    onClick: () => void;
};
export declare const AbstractConnectorButton: ({ onClick, disabled, additionalStyles, children, }: Props) => JSX.Element;
export {};
