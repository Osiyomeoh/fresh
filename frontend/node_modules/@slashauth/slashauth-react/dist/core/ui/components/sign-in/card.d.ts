/// <reference types="react" />
declare type Props = {
    showBackButton?: boolean;
    children: React.ReactNode;
};
export declare const SignInCard: ({ showBackButton, children }: Props) => JSX.Element;
export {};
