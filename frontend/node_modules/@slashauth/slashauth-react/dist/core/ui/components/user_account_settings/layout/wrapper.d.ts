import React from 'react';
export declare const Wrapper: <C extends React.ElementType<any> = "main">({ children, component, }: {
    children: React.ReactNode;
    component?: C;
} & React.PropsWithoutRef<React.ComponentProps<C>>) => JSX.Element;
