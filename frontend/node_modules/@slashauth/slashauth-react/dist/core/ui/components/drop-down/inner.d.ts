/// <reference types="react" />
import { SlashAuth } from '../../../slashauth';
import { User } from '../../../user';
type Context = Pick<SlashAuth, 'openSignIn' | 'logout' | 'isReady' | 'connectWallet'> & {
    appName: string;
    user: User;
};
type Props = {
    context: Context;
};
export declare const Inner: ({ context }: Props) => JSX.Element;
export {};
