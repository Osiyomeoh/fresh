import { Web3LoginMethod } from '../../context/login-methods';
declare type Props = {
    loginMethod: Web3LoginMethod;
    onClick: () => void;
};
export declare const WalletConnectorButton: ({ loginMethod, onClick }: Props) => JSX.Element;
export {};
