import { ReactEventHandler } from 'react';
export declare const LoadingScreen: ({ navigateBack, loading, description, detailedDescription, }: {
    navigateBack: ReactEventHandler;
    loading?: boolean;
    description: string;
    detailedDescription: string | React.ReactNode;
}) => JSX.Element;
