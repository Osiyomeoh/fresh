import React from 'react';
type Props = {
    children: React.ReactNode;
    onClick: (e: React.MouseEvent) => void;
};
export declare const ModalBackdrop: ({ children, onClick }: Props) => JSX.Element;
export {};
