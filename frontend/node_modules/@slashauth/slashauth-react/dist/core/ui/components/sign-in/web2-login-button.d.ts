import { LoginMethod } from '../../context/login-methods';
declare type Props = {
    display: 'full' | 'icon';
    loginMethod: LoginMethod;
    onClick: (loginMethod: LoginMethod) => void;
};
export declare const Web2LoginButton: ({ loginMethod, display, onClick }: Props) => JSX.Element;
export {};
