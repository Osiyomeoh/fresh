import React from 'react';
interface BaseRouterProps {
    basePath: string;
    startPath: string;
    getPath: () => string;
    getQueryString: () => string;
    internalNavigate: (toURL: URL) => Promise<any> | any;
    onExternalNavigate?: () => any;
    refreshEvents?: Array<keyof WindowEventMap>;
    preservedParams?: string[];
    children: React.ReactNode;
}
export declare const BaseRouter: ({ basePath, startPath, getPath, getQueryString, internalNavigate, onExternalNavigate, refreshEvents, preservedParams, children, }: BaseRouterProps) => JSX.Element;
export {};
